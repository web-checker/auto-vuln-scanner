# 기본적으로 모든 파일 무시
*

# 디렉터리는 추적 가능하도록 허용 (하위 파일 탐색을 위해 필요)
!*/

# 실행 스크립트 및 설정 파일만 허용
!*.sh
!*.ps1
!*.conf

# .gitignore / .gitattributes 자체는 추적
!.gitignore
!.gitattributes

# 공통 README(루트)만 문서로 추적 — 결과물/기타 .md 는 계속 무시
!/README.md
